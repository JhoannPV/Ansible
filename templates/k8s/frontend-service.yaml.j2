apiVersion: v1
kind: Service
metadata:
  name: calendar-frontend
  namespace: {{ K8S_NAMESPACE }}
  labels:
    app: calendar-frontend
spec:
  type: ClusterIP
  selector:
    app: calendar-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
